name = "pm-skills-telegram"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Telegram bot → run a professional skill from any Telegram chat.
# Deploy:  cd integrations/telegram && npx wrangler deploy
# Secrets: npx wrangler secret put TELEGRAM_BOT_TOKEN     # from @BotFather
#          npx wrangler secret put TELEGRAM_WEBHOOK_SECRET # any random string; also passed to setWebhook
#          npx wrangler secret put ANTHROPIC_API_KEY       # optional — enables /run
