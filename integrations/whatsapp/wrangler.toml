name = "pm-skills-whatsapp"
main = "src/index.js"
compatibility_date = "2025-01-01"

# WhatsApp bot via Twilio webhook. Replies are TwiML in the webhook response,
# so no Twilio credentials live here.
# Deploy:  cd integrations/whatsapp && npx wrangler deploy
# Secret (optional, enables "run"): npx wrangler secret put ANTHROPIC_API_KEY
